/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ["./src/**/*.{html,js}"], //we are adding this to tell tailwind just to update these extensions like under folder src and for any further folders to search for js and html files to apply tailwind
  theme: {
    extend: {},
  },
  plugins: [],
};
